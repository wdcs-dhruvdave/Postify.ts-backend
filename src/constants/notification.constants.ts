export const NOTIFICATION_MESSAGES = {
  SERVER_ERROR_FETCHING: "Server error while fetching notifications",
  SERVER_ERROR_MARKING_AS_READ: "Server error while marking notifications as read",
};

export const NOTIFICATION_CONFIG = {
  LIMIT: 20,
  SORT_ORDER: -1,
};